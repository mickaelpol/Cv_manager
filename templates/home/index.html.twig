{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}

    <div
            data-action="modal-form:success->reload-content#refreshContent"
            {{ stimulus_controller('reload-content', {
                url: path('app_home', { ajax: 1 })
            }) }}
    >
        <div class="row mt-2">
            <div class="col">
                <h1 class="text-center">Accueil</h1>
                <h2 class="text-center mt-5">Créer vos CV dynamique</h2>
            </div>
        </div>

        <div class="row mt-5">
            {{ include('modal/new_cateogie.html.twig') }}
            {{ include('modal/new_content.html.twig') }}
        </div>

        <div data-reload-content-target="content">
            {{ include('home/_list.html.twig') }}
        </div>
    </div>


{% endblock %}
